// flowers: deterministic, dependency-free botanical SVGs. One seed, one
// flower, the same on every machine, forever. The garden grows a single
// species today, the plumeria; another would live beside it, sharing
// `shared/` and never touching this one.

export {
  cultivar,
  type PlumeriaOptions,
  plumeria,
  type Theme,
} from "./plumeria";
